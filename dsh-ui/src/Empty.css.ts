/** Style segment for {@link ././Empty.tsx Empty} — leaf module, imported only by css.ts. */
export const emptyCss = `
.dshui-empty { display: flex; flex-direction: column; gap: 4px; }
.dshui-empty-title { font-size: 14px; font-weight: 600; color: var(--dshT3-fg); }
.dshui-empty-description { margin: 0; font-size: 13px; color: var(--dshT3-fg-muted); line-height: 1.5; }
`
