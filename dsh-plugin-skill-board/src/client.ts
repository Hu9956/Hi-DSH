/**
 * Hi-DSH Skill Board — Client side (v0.1 placeholder)
 * Renders a simple list in Settings > Skill Board.
 * Full beautiful UI comes after Hot toggle is proven.
 */
export const name = 'dsh-plugin-skill-board/client'
export function apply() {
  // In v0.1 we only mount the host toggle logic.
  // Client UI will be added after Step 4 verification: a Settings panel that
  // fetches /api/skill-board/list and POST /api/skill-board/toggle.
  // Keeping this file ensures the bundle is valid.
}
